#include "LinkedList.hpp"


using namespace std;

ll->indexOf(1);

void indexOf(int value)
{
    int valueO = value;
    ll->getAtIndex(value);
    if(valueO != value)
    {
        return value;
    }
    else
    {
        return -1;
    }
}